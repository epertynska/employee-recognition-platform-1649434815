<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
    <a class="navbar-brand" href="/home/index">The Employee Recognition App</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        <%# <li class="nav-item active">
            <a class="nav-link" href="/kudos/kudos">Kudos</a>
        </li>
        <li class="nav-item active">
            <a class="nav-link" href="/kudos/create_kudo">Create new Kudo</a>
        </li>
        <li class="nav-item active">
            <a class="nav-link" href="/kudos/your_kudos">Your Kudos</a>
        </li>
        </ul> %>
        <p class="navbar-text float-right">
        <% if employee_signed_in? %>
        Logged in as <strong><%= current_employee.email %></strong>.
        <%= link_to 'Edit profile', edit_employee_registration_path, :class => 'navbar-link' %> |
        <%= link_to "Logout", destroy_employee_session_path, method: :delete, :class => 'navbar-link'  %>
        <% else %>
        <%= link_to "Sign up", new_employee_registration_path, :class => 'navbar-link'  %> |
        <%= link_to "Sign in", new_employee_session_path, :class => 'navbar-link'  %>
        <% end %>
        </p>
    </div>
    </div>
</nav>
<% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <div class="container">